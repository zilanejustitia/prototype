# Campusly Static Prototype

Buka `index.html` langsung di Google Chrome. Tidak perlu Node.js, npm, server, akun, pembayaran, atau database.

Semua data adalah contoh. Interaksi keranjang, login demo, form seller, dan checkout menggunakan `localStorage` browser saja. Biaya transaksi prototype adalah 5%.
